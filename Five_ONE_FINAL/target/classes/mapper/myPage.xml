<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="myPageDAO">

	<select id="history_list" resultType="videoplayDTO" parameterType="String">
		select * from video_play where video_code in (select video_code from history where channel_code = #{code}) order by video_regdate desc
	</select>
	
	<select id="playlist_list" resultType="playlistDTO" parameterType="String">
		select distinct playlist_no, playlist_title from playlist where channel_code = #{code}
	</select>
	
	<select id="good_list" resultType="videoplayDTO" parameterType="String">
		select * from video_play where video_code in (select video_code from good where channel_code = #{code}) order by video_regdate desc
	</select>
	
	
	<select id="member_channel" resultType="channeldto" parameterType="String">
		select * from user_channel where channel_code = #{code}
	</select>
	
	<select id="channel_video" resultType="videoplayDTO" parameterType="String">
		select * from video_play where channel_code = #{code} order by video_regdate desc
	</select>
	
	<select id="channel_sub" resultType="videoplayDTO" parameterType="String">
		select * from subscribe where channel_code = #{code}
	</select>
	
	<select id="reply_list" resultType="replyDTO" parameterType="String">
		select * from reply where channel_code = #{code}
	</select>
	
	<delete id="delete_history" parameterType="String">
		delete from history where channel_code = #{code}
	</delete>
	
	<select id="history_search" parameterType="Map" resultType="videoplayDTO">
		select * from video_play where video_code in (select video_code from history where channel_code = #{code}) and video_title like '%' || #{keyword} || '%' order by video_regdate desc
	</select>
	
	<delete id="history_one_delete" parameterType="Map">
		delete from history where channel_code = #{channel_code} and video_code = #{video_code}
	</delete>
	
	<select id="getHistory_num" parameterType="Map" resultType="int">
		select history_num from history where channel_code = #{channel_code} and video_code = #{video_code}
	</select>
	
	<update id="updateSeq" parameterType="int">
		<![CDATA[update history set history_num = history_num - 1 where history_num > #{num}]]>
	</update>
	
	<select id="playlist_no" parameterType="Map" resultType="videoplayDTO">
		select * from video_play where video_code in (select video_code from playlist where channel_code = #{code} and playlist_no = #{no})
	</select>
	
	<select id="playlist_search" parameterType="Map" resultType="videoplayDTO">
		select * from video_play where video_code in (select video_code from playlist where channel_code = #{code} and playlist_no = #{no}) and video_title like '%' || #{keyword} || '%'
	</select>
	
	<delete id="playlist_one_delete" parameterType="Map">
		delete from playlist where channel_code = #{channel_code} and video_code = #{video_code} and playlist_no = #{playlist_no}
	</delete>
	
	<select id="getPlaylist_num" parameterType="Map" resultType="int">
		select playlist_num from playlist where channel_code = #{channel_code} and video_code = #{video_code} and playlist_no = #{playlist_no}
	</select>
	
	<update id="updateSeq_p" parameterType="int">
		<![CDATA[update playlist set playlist_num = playlist_num - 1 where playlist_num > #{num}]]>
	</update>
	
	<delete id="delete_playlist" parameterType="Map">
		delete from playlist where channel_code = #{code} and playlist_no = #{no}
	</delete>
	
</mapper>

