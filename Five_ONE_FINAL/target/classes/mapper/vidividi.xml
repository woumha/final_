<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="VidividiDAO">
	<select id="test" parameterType="String" resultType="TestDTO">
		select * from test1130
	</select>
	
	<!-- main video list 출력 -->
	<select id="video_list" parameterType="String" resultType="VideoPlayDTO">
		select video_title, video_cont, video_view_cnt, video_regdate from video_play order by video_good
	</select>
	
</mapper>