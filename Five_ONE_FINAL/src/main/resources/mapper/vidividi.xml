<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="VidividiDAO">
	<select id="test" parameterType="String" resultType="TestDTO">
		select * from test1130
	</select>
	
	<!-- main video list 출력(인기순) -->
	<select id="video_list" parameterType="String" resultType="VideoPlayDTO">
		select video_title, video_cont, channel_name, video_view_cnt, video_regdate, video_code from video_play order by video_good
	</select>
	
	<!-- main video list 출력(최신순) -->
	<select id="video_list_up" parameterType="String" resultType="VideoPlayDTO">
		select video_title, video_cont, channel_name, video_view_cnt, video_regdate, video_code from video_play order by video_regdate
	</select>
	
	
	<select id="searchVideo" parameterType="hashmap" resultType="VideoPlayDTO">
		select * from video_play where ${field} 
		like '%' || #{keyword} || '%' order by video_good
	</select>
	
	
	<select id="ch_name" parameterType="hashmap" resultType="VideoPlayDTO">
		select * from video_play where ${field} 
		like '%' || #{keyword} || '%' order by video_good
	</select>
	
	
	<select id="vi_hash" parameterType="hashmap" resultType="VideoPlayDTO">
		select * from video_play where ${field}
		like '%' || #{keyword} || '%' order by video_good
	</select>
	
</mapper>